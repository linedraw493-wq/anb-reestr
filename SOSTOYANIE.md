---
title: Реестр блогеров — где остановились
type: state
status: active
date: 2026-09-02
---

# Где остановились

**Читать первым делом в новой сессии.** Здесь то, что нельзя восстановить из
кода: что решено, что сделано, что делать дальше и чего ждём от владельца.
Решения по порядку — в `voprosy-i-dyry.md`, устройство — в `app/README.md`.

## Поднять стенд

```
bash app/tools/podnyat.sh     # база, сервер, экраны
bash app/tools/opustit.sh     # остановить
```

Открывается на `http://localhost:5173`. Если Docker Desktop лёг — запустить
`C:\Users\admin\AppData\Local\Programs\DockerDesktop\Docker Desktop.exe` и
подождать движок. Это случалось трижды, не пугаться.

**Владелец — админ:** `+7 705 281 93 42`. Коды приходят **в Telegram**
владельцу, в бот `@Kikokpklkbot`, чей бы номер ни вводили — так устроена
временная замена SMS.

## Сделано и проверено вживую

- Вход: приглашение по ссылке, код в Telegram, сессия 60 дней, роли
  блогер/модератор/админ. Заготовка без телефона просит вписать свой.
- Карточка: форма и живой предпросмотр, ссылки вставкой, фото и скрин
  ужимаются и лежат в базе. Причина возврата доходит до блогера.
- Разрез полей: ник, ссылки, тематика, ставка правятся сразу; цифры и скрин
  у опубликованной карточки уходят на проверку, в каталоге висят старые.
- Модерация: очередь по вкладкам, правка, одобрить/вернуть/удалить/завести,
  журнал решений.
- **Каталог:** фильтры за кнопкой (карточки во всю ширину), поиск, четыре
  сортировки, страницы, карта Казахстана на OpenStreetMap, страница блогера
  с ценой за тысячу охвата. Фильтры живут в адресной строке.
- **Инвайты и коды из админки** — `/moderator/priglasheniya`: 307 ссылок с
  состоянием, копирование, выпуск новой, выгрузка таблицей, **резервный код**
  (показывается админу, в Telegram не уходит).
- **Скрыть карточку** — обратимо, вместо только «удалить навсегда».
- **Списки** `/moderator/spiski` — правка, скрытие, **слияние**.
- **Сводка** `/moderator/svodka` — воронка от ссылок до каталога.
- **Главная:** цифры из базы, две двери, «как это идёт», подборки, подвал.
- **Телефон:** фильтры каталога в шторке снизу.
- База: 19 таблиц, 6 миграций, накатываются сами при старте.
- Данные заказчика: 306 блогеров, 305 живых приглашений, 433 сырые строки.

## Что делать дальше — по порядку

Слово владельца 02.09: **первый этап доделать и вылизать, потом второй.**

### Доделать первый этап

1. **Загрузка и пустые состояния** — сейчас сетка просто блёкнет. Нужны
   скелетоны или честный «ищем».
2. **Постраничная выдача у модератора** — там всё ещё 306 одним куском.
   Сервер уже умеет `stranica`/`na_stranice`, надо подключить экран.

### Второй этап — решено владельцем 02.09

3. **Выдача приглашений модератору** — экран `/moderator/priglasheniya`:
   список 305 ссылок, состояние (не открывали · открыли · зарегистрировался
   · просрочено), «скопировать», перевыпуск. Отметка `otkryto_v` уже
   пишется. **Дашборд со счётчиками владелец отложил** — «пока лишнее».
4. **Админка списков** `/moderator/spiski` — тематики, города, районы:
   добавить, переименовать, скрыть (не удалять), «стоит в N карточках»,
   **слияние**.
5. **Готовые причины отказа** — шесть штук лежат в `prichiny_otkaza`, к
   экрану модератора не подключены.

### Дальше по этапам

Уведомления · настоящий SMS · Railway · ИИ-чтение скрина · согласие и
политика · кнопка «удалить себя» · инструкция · передача доступов.
Полный список — артефакт «Дорога до сдачи».

## Ждём от владельца

- **Настоящие списки тематик и районов.** Те, что стоят, — «что попало для
  вида», его слово. Менять **до** того, как блогеры начнут заполнять.
- **Railway** — около 5 $/мес, бесплатные места заняты «Лидером».
- **SMS-оператор заказчика** — регистрация имени отправителя в КЗ идёт
  недели, начинать заранее.
- **Ключ Anthropic** — для ИИ-чтения.
- **Текст согласия и политики** — от юриста заказчика.

## Грабли, на которые уже наступали

- **Сервер крутит старый код.** `pkill -f uvicorn` не убивает — убивать по
  номеру процесса через `netstat -ano | grep 127.0.0.1:8000`.
- **Кириллица в аргументах командной строки ломается** — консоль не UTF-8.
  SQL и скрипты подавать через файл или поток, вывод держать в латинице.
- **Пути в Git Bash.** `docker cp /tmp/x.sql` превращается в windows-путь.
  Подавать через `docker exec -i ... < файл`.
- **Docker Desktop сам гаснет.** База переживает, сессия в браузере — нет.
- **Тестовые данные.** Ники в таблице настоящие, цифры выдуманные. Показывать
  такое заказчику нельзя. Наливка и уборка — `app/tools/test-dannye.py`
  с `--nalit` и `--ubrat`. Сейчас **убрано**, в каталоге 2 карточки.

## Выгружено

Закрытый репозиторий: `github.com/linedraw493-wq/anb-reestr`, ветка `main`.
Личных данных и секретов в нём нет — проверено перед пушем.

## Границы

- Личные данные 306 блогеров — в базе и в `data/`, **в git не едут никогда**.
- Наружу ничего не деплоится без слова владельца.
- Токен бота и ключи — в `app/.env`, вне git.
- Токен бота светился в переписке 02.09; владелец сказал не страшно,
  перевыпуск отложен.

## Карты

Решения по раундам — `voprosy-i-dyry.md` · устройство и выключатели —
`app/README.md` · дверь проекта — `CLAUDE.md`.
