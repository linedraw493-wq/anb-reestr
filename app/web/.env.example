# Кто отвечает экранам (приглашения, карточки, заявки).
#   fake — заглушка в браузере, сервера не надо
#   live — настоящий сервер по /api (нужна база, будет на Railway)
VITE_API=fake

# Коды входа.
#   fake — код всегда 000000
#   live — код уходит в Telegram-бота через /api/otp
VITE_OTP=fake
