/* ---------------------------------------------------------------------------
   Один выключатель на весь фронт:

     VITE_API=fake — заглушка в браузере, сервер не нужен
     VITE_API=live — настоящий сервер по /api

   Живёт отдельным файлом, чтобы модули не ссылались друг на друга по кругу.
--------------------------------------------------------------------------- */

export const USE_FAKE = (import.meta.env.VITE_API ?? 'fake') === 'fake'
