"""Точка входа для Vercel.

На своей машине сервер поднимает uvicorn (`tools/podnyat.sh`). На бою его
поднимает Vercel: он ищет в этом файле переменную `app` и разговаривает с
ней напрямую. Своего кода здесь нет — только показать дорогу к серверу.

Путь дописываем руками: Vercel запускает файл из корня проекта, а сервер
лежит рядом, в папке `app`. Без этой строки импорт бы не нашёлся.
"""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from app.main import app  # noqa: E402  (после правки пути — иначе не найдёт)

__all__ = ["app"]
